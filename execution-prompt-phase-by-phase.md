# EXECUTION PROMPT — تنفيذ مرحلي مع بوابة تحقق (Phase-by-Phase Execution with Verification Gate)

> يُستخدم هذا البرومبت **بعد** رفع/توفير ملف `master-prompt-car-rental-platform.md` في نفس الجلسة أو المشروع.

---

## التعليمات

اقرأ الملف `master-prompt-car-rental-platform.md` بالكامل أولاً قبل تنفيذ أي شيء. هذا الملف هو مرجعك الوحيد للمتطلبات والمعمارية والقيود — لا تخرج عنه ولا تتجاهل أي قسم منه.

نفّذ المشروع **مرحلة واحدة في كل مرة فقط**، باتباع الترتيب الوارد في قسم "مراحل التطوير" (PHASE 0 إلى PHASE 6) في الملف. لا تنتقل إلى مرحلة تالية إلا بعد إتمام كل الخطوات التالية للمرحلة الحالية:

### لكل مرحلة، اتبع هذا التسلسل بالضبط:

1. **أعلن بداية المرحلة**: اذكر رقم المرحلة واسمها، والأهداف المحددة لها كما وردت في الملف.
2. **نفّذ فقط** ما هو مطلوب في هذه المرحلة — لا تُنفّذ أعمالاً من مراحل لاحقة حتى لو بدت مرتبطة، ولا تتجاوز أي بند مطلوب في هذه المرحلة.
3. **تحقّق فعلياً من النجاح** (لا تفترض النجاح، بل نفّذ الأوامر واقرأ مخرجاتها فعلياً):
   - `npm run lint`
   - `npx tsc --noEmit`
   - `npm run build`
   - أي فحص إضافي خاص بالمرحلة (مثال: PHASE 1 → تشغيل الـ migrations والتحقق من الاتصال بقاعدة البيانات وتشغيل الـ seed؛ PHASE 2 → التحقق يدويًا/برمجيًا أن الصفحات تُبنى وتُعرض بدون أخطاء وأن العربية/الإنجليزية وRTL/LTR تعمل؛ PHASE 3 → التحقق من وجود sitemap وrobots.txt وmetadata الديناميكي؛ PHASE 6 → التحقق من أن الـ webhook هو مصدر الحقيقة الوحيد لحالة الدفع).
4. **إذا فشل أي فحص**: أصلح السبب الجذري للمشكلة ضمن نفس المرحلة، ثم أعد تشغيل جميع الفحوصات من جديد. لا تنتقل للمرحلة التالية وأنت تعلم أن هناك فحصًا فاشلاً، ولا "تُرقّع" المشكلة بحل مؤقت يخالف قسم "قيود مهمة" في الملف.
5. **حدّث ملف `PROGRESS.md`** في جذر المشروع (أنشئه إن لم يكن موجودًا) بالتنسيق التالي لكل مرحلة مكتملة:

   ```markdown
   ## PHASE X — [اسم المرحلة] ✅ [التاريخ/الوقت]

   ### ما تم تنفيذه
   -

   ### الملفات المُنشأة/المُعدّلة
   -

   ### تغييرات قاعدة البيانات
   -

   ### الاعتماديات المُضافة
   -

   ### الفحوصات المُنفَّذة ونتائجها
   - lint: ✅/❌
   - tsc --noEmit: ✅/❌
   - build: ✅/❌
   - [فحوصات خاصة بالمرحلة]: ✅/❌

   ### مشكلات معروفة/متبقية
   -
   ```

6. **اعمل commit** بعد نجاح المرحلة، باستخدام رسالة بأسلوب قسم "سير عمل Git" في الملف (مثال: `feat: complete phase 1 - foundation & database`).
7. **قدّم تقريرًا موجزًا للمستخدم** بنفس بنود `PROGRESS.md` أعلاه، ثم اسأل: **"هل أنتقل إلى [اسم المرحلة التالية]؟"** وانتظر التأكيد قبل البدء بالمرحلة التالية — **إلا إذا** طلب المستخدم صراحة في بداية هذه الجلسة تنفيذ كل المراحل تلقائيًا دون توقف بينها (auto-continue)، وفي هذه الحالة انتقل مباشرة بعد نجاح الفحوصات مع الإبقاء على التقرير الموجز بعد كل مرحلة.

### قواعد صارمة أثناء التنفيذ

- **PHASE 0 إلزامية ولا يجوز تخطيها.** لا تبدأ كتابة كود إنتاجي قبل عرض خطة PHASE 0 (فحص المشروع، القرارات المعمارية، المخاطر) والحصول على توافق ولو ضمني (أو تنفيذها إذا طلب المستخدم عدم التوقف).
- لا تتجاوز حدود المرحلة الحالية حتى لو كان تنفيذ جزء من مرحلة لاحقة "أسهل الآن" — الانضباط بالترتيب يمنع تشابك الأخطاء.
- إذا اكتشفت أثناء التنفيذ تعارضًا بين متطلبين في الملف الأصلي، **قف واعرض التعارض بوضوح** واقترح الحل الأنسب معماريًا بدلاً من الاختيار الصامت.
- لا تُعلن أي ميزة بأنها "تعمل" ما لم تتحقق منها فعليًا بالأوامر أعلاه أو باختبار مباشر.
- عند أي فشل غير قابل للحل ضمن نفس المرحلة بعد محاولتين، توقف واشرح للمستخدم طبيعة العائق بدل الاستمرار بحل مؤقت.
- التزم بكل ما ورد في قسم "قيود مهمة" و"قاعدة اتخاذ القرار" من الملف الأصلي طوال التنفيذ.

---

## ابدأ الآن بـ PHASE 0 — التحليل.
